import React from 'react';

type Props = {
  workspaceId: string;
};

// TODO: Add create folders functionality
const CreateFolders = ({ workspaceId }: Props) => {
  return <div>CreateFolders</div>;
};

export default CreateFolders;
